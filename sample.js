const script = () => {
    let x = 100;
    let y = 5;

    c = x + y;
    console.log(c);

    const add = (a, b) => {
        let c = a + b;

        console.log(c)
    }

    const multiply = (a, b) => {
        let c = a * b;

        console.log(c)
    }

    add(90, 10)
    multiply(8, 100)
}